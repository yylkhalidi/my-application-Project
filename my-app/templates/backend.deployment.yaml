apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend.name }}
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: {{ .Values.backend.image }} # TO DO check values.yaml
        ports:
        - containerPort: {{ .Values.backend.servicePort }} # 8080
        env:
        - name: SPRING_POSTGRES_USERNAME
          value: avnadmin
        - name: SPRING_POSTGRES_PASSWORD
          value: AVNS_fLGVxXhUXAZiW13mk5y
        - name: SPRING_POSTGRES_URL
          value: jdbc:postgresql://pg-694830e-yy-7535.a.aivencloud.com:21195/defaultdb # TO ADD 